{% extends 'base.html.twig' %}

{% block title %}Ducktable{% endblock %}

{% block body %}
    <h1>Ducktable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ducktable.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ ducktable.Firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ ducktable.Lastname }}</td>
            </tr>
            <tr>
                <th>Duckname</th>
                <td>{{ ducktable.Duckname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ ducktable.Email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ ducktable.Password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ducktable_index') }}">back to list</a>

    <a href="{{ path('ducktable_edit', {'id': ducktable.id}) }}">edit</a>

    {{ include('ducktable/_delete_form.html.twig') }}
{% endblock %}
